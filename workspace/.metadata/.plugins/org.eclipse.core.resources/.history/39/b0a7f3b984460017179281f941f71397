import player.SimplePlayer;
import tag.Host;

public class Main {

	public static void main(String[] args) {
		Host host=new Host();
		
		host.setPlayer(0,new SimplePlayer());
		host.setPlayer(0,new SimplePlayer());

	}

}
