package player;

import tag.Data.Point;

import org.neuroph.core.NeuralNetwork;
import org.neuroph.nnet.MultiLayerPerceptron;

import tag.Player;
import tag.Viewer;

public class SimplePlayer extends Player {
	private static NeuralNetwork neuralNetwork;
	public SimplePlayer(){
		this.neuralNetwork=new MultiLayerPerceptron(30*30,100,30*30);
		
	}
	
	

	@Override
	public Point play(Viewer v) {
		
		
		
		
		return null;
	}
	
	private double[] getDoubleArray(Viewer v){
		double[] ret=new double[v.getWidth()*v.getHeigth()];
		
		for(int i=0;i<v.getWidth();i++){
			for(int j=0;j<v.getHeigth();j++){
				
			}
		}
		
		
		return ret;
	}

}
